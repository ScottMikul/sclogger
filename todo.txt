1) Create a view that shows all of the blog entries. x
2) Create a view that allows for users to update or add blog entries. x
3) Connect to a webserver and test some basic rest services. x
4) make an http read service call to pull the blog entries, then update the view. x
5) store some data in mongodb, run a read service to pull the blog entries, then update the view